name: Publish to AWS S3

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  AWS_REGION: us-east-1
  AWS_S3_BUCKET_NAME: cl-ltr-new
  CLOUDFRONT_DISTRIBUTION_ID: E2LR5BJBP0NWCL
  
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/ciscodocs/ci-image-v2:latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::963057650703:role/GitHubAction-MkDocsPublish
          aws-region: ${{ env.AWS_REGION }}

      # The prebaked container ships with mkdocs + mkdocs-material + a small
      # baseline of plugins, but not the lab-specific extras (glightbox,
      # with-pdf, etc.). Install the full pinned list from requirements.txt
      # so the build is reproducible and survives image refreshes.
      - name: Install dependencies
        run: pip install --no-cache-dir -r requirements.txt

      - name: Build
        run: mkdocs build --clean

      - name: Publish
        run: |
          aws s3 sync site s3://${{ env.AWS_S3_BUCKET_NAME }}/6693591399

      - name: Invalidate CloudFront Cache
        if: env.CLOUDFRONT_DISTRIBUTION_ID != ''
        run: |
          aws cloudfront create-invalidation --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} --paths "/6693591399/*"
        continue-on-error: true
